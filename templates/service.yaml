apiVersion: v1
kind: Service
metadata:
  name: {{ include "reatjs-plants-chart.fullname" . }}
  labels:
    {{- include "reatjs-plants-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reatjs-plants-chart.selectorLabels" . | nindent 4 }}
