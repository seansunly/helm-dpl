apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reatjs-plants-chart.fullname" . }}-test-connection"
  labels:
    {{- include "reatjs-plants-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reatjs-plants-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
